/*
const express= require('express');
const app = express();

app.use('/', function(req, res){
  res.sendFile(__dirname + '/index.html');
});

 app.listen(8083, ()=>{
   console.log("listening 8083")

 });
*/


/*Esto lo comento porque en la linea 44 de gulpfile levanto el servidor abreviadamente con el siguiente codigo
browserSync.init({ 
        server: {
            basedir: './public'
        }
    })*/
